apiVersion: v1
kind: Service
metadata:
  name: {{ include "dockerizer-backend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dockerizer-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "dockerizer-backend.selectorLabels" . | nindent 4 }}
  ports:
    {{- range .Values.service.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol | default "TCP" }}
    {{- end }}
