apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-{{ .Values.envFrom.secretSuffix }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dockerizer-backend.labels" . | nindent 4 }}
stringData:
  SPRING_DATASOURCE_PASSWORD: {{ .Values.applicationYaml.spring.datasource.password | quote }}
  {{- if .Values.applicationYaml.dockerizer.opensearch.password }}
  DOCKERIZER_OPENSEARCH_PASSWORD: {{ .Values.applicationYaml.dockerizer.opensearch.password | quote }}
  {{- end }}
