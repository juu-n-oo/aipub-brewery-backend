apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "dockerizer-backend.fullname" . }}
  labels:
    {{- include "dockerizer-backend.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "dockerizer-backend.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "dockerizer-backend.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
